# Preparation stub for the 26.4 cycle (mirrors the old 26.3-snapshot-7 workflow:
# minecraft_version pins the exact snapshot, fabric_api_version tracks the +26.4 target line).
# TODO: once Mojang publishes 26.4-snapshot-1 and Fabric ships a +26.4 build,
# bump both values below, then run ./gradlew genSources to diff vanilla API changes.
minecraft_version=26.4-snapshot-1
fabric_api_version=0.161.0+26.4
